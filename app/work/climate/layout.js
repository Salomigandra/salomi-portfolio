export const metadata = {
  title: "How Much Has Your Country Warmed? — Global Climate Data | Salomi Gandra",
  description:
    "A data story mapping how much every region has warmed since pre-industrial times, who emits what, and what the trajectory looks like — built on NASA, WMO, IPCC, and Global Carbon Project data.",
};

export default function ClimateLayout({ children }) {
  return children;
}
